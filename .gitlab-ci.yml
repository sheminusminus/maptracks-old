before_script:
  - npm install

stages:
  - clean
  - build

clean:
  stage: clean
  script:
    - npm run clean
  only:
    - master

build:
  stage: build
  script:
    - npm run build-api
    - npm run build-client
  only:
    - master
