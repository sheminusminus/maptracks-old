import {
  login,
  register,
  logout,
} from './auth';

import {
  getFirebaseConfig,
} from './firebase';

import {
  snapToRoads,
} from './map';

import {
  getSpotifyUrl,
} from './spotify';

export {
  // auth
  login,
  register,
  logout,
  // firebase
  getFirebaseConfig,
  // maps
  snapToRoads,
  // spotify
  getSpotifyUrl,
};
